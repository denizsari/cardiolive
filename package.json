{
  "name": "cardiolive",
  "version": "1.0.0",
  "description": "Modern zeytinyağı e-ticaret platformu",
  "main": "ecosystem.config.js",
  "scripts": {
    "install": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "test:integration": "jest tests/e2e-integration.test.js",
    "test:performance": "k6 run performance-tests/load-test.js",
    "test:security": "node security-tests/penetration-test.js",
    "test:all": "npm run test && npm run test:integration",
    "clean": "rm -rf frontend/.next backend/dist node_modules/*/node_modules",
    "lint": "npm run lint --prefix frontend && npm run lint --prefix backend",
    "lint:fix": "npm run lint:fix --prefix frontend && npm run lint:fix --prefix backend",
    "format": "npm run format --prefix frontend && npm run format --prefix backend",
    "format:check": "npm run format:check --prefix frontend && npm run format:check --prefix backend",
    "docker:dev": "docker-compose up -d",
    "docker:prod": "docker-compose -f docker-compose.prod.yml up -d",
    "deploy": "bash deployment/deploy.sh",
    "setup:final": "node .github/scripts/final-setup.js",
    "setup:branch-protection": "node .github/scripts/setup-branch-protection.js",
    "setup:deployment": "node .github/scripts/setup-deployment.js",
    "test:workflow": "node .github/scripts/test-workflow.js",
    "verify:hydration": "node .github/scripts/verify-hydration-fixes.js",
    "version:patch": "npm version patch && git push origin development --tags",
    "version:minor": "npm version minor && git push origin development --tags",
    "version:major": "npm version major && git push origin development --tags",
    "release:prepare": "git checkout main && git pull && git merge development",
    "release:finish": "git push origin main && git checkout development"
  },
  "keywords": [
    "e-commerce",
    "zeytinyağı",
    "next.js",
    "express.js",
    "typescript",
    "mongodb"
  ],
  "author": "Cardiolive Team",
  "license": "MIT",
  "dependencies": {
    "@octokit/rest": "^22.0.0",
    "axios": "^1.9.0",
    "concurrently": "^8.2.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
